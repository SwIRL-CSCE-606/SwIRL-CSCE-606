<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #e2c9be;
            background-image: linear-gradient(315deg, #e2c9be 0%, #fbf7e9 74%);
            background-size: cover;
            background-repeat: no-repeat;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Arial', sans-serif;
        }

        .event-card {
            background-color: rgba(255, 255, 255, 0.9);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
            border-radius: 15px;
            overflow: hidden;
            transition: transform 0.3s;
        }

        .event-card:hover {
            transform: translateY(-10px);
        }

        .gradient-btn {
            background: linear-gradient(45deg, #4CAF50, #44A1F2);
            border: none;
            color: white;
            padding: 12px 30px;
            border-radius: 30px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            transition: all 0.3s;
        }

        .gradient-btn:hover {
            background: linear-gradient(45deg, #44A1F2, #4CAF50);
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.4);
            transform: translateY(-2px);
        }

        .event-header {
            padding: 30px 20px;
            background-color: #4CAF50;
            color: white;
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.2);
            font-size: 2em;
            font-weight: bold;
            letter-spacing: 2px;
        }

        .event-content {
            padding: 30px 20px;
        }

        .highlight-text {
            color: rgb(8, 92, 120);
            padding: 5px 10px;
            border-radius: 10px;
            font-size: 1.5em;
            font-weight: bold;
        }

    </style>
</head>

<body>
    <div class="event-card">
        <div class="event-header">
            Event Invitation
        </div>
        <div class="event-content">
            <p class="text-center mb-4 highlight-text">You are cordially invited to our special event:</p>
            <p class="mb-3"><strong>Event Name:</strong> <%= @event.name %> </p>
            <p class="mb-3"><strong>Date:</strong> <%= @event.event_info.date.strftime('%B %d, %Y') %></p>
            <p class="mb-3"><strong>Time:</strong> <%= @event.event_info.time.strftime('%H:%M %p') %> </p>
            <p class="mb-3"><strong>Location:</strong> <%= @event.event_info.venue %></p>
            <p class="mb-4"><strong>Description:</strong> <%= @event.event_info.description %></p>
            <div class="d-flex justify-content-around">
                <button class="gradient-btn">Yes, I'll Attend</button>
                <button class="btn btn-outline-danger">No, I Can't Make It</button>
            </div>
        </div>
    </div>
</body>

    <!-- Future: Bootstrap JS and dependencies -->
    <!-- 
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    -->

</html>
