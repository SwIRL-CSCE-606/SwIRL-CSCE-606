<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Skhedule.com - Event Management</title>
    <!-- Bootstrap CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Arial', sans-serif;
            background-color: black;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: white;
        }

        .event-card {
            background-color: black;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.5);
            border-radius: 15px;
            padding: 20px;
            margin: 20px auto;
            max-width: 960px;
            width: 80%;
            max-height: 90vh;
            overflow-y: auto;
            transition: transform 0.3s;
        }

        .event-card:hover {
            transform: scale(1.02);
        }

        .gradient-btn {
            background: linear-gradient(45deg, #4CAF50, #44A1F2);
            border: none;
            color: white;
            padding: 12px 30px;
            font-size: 18px;
            text-transform: uppercase;
            letter-spacing: 2px;
            border-radius: 5px;
            transition: background-color 0.3s, transform 0.3s;
            margin: 10px auto;
            width: 250px;
            display: block;
            margin-bottom: 20px;
        }

        .gradient-btn:hover {
            background-color: #444;
            cursor: pointer;
            transform: scale(1.05);
        }

        .title {
            font-size: 40px;
            font-weight: bold;
            margin-bottom: 30px;
        }

        .subtitle {
            font-size: 24px;
            margin-bottom: 25px;
        }

        label {
            color: white;
            display: block;
            margin-top: 20px;
        }

        .form-input {
            background: #222;
            color: #ddd;
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            border: 1px solid #333;
            box-sizing: border-box;
        }

        @media (max-width: 768px) {
            .event-card {
                width: 95%;
                padding: 10px;
                margin: 10px auto;
                max-height: 90vh;
            }

            .form-input, .gradient-btn {
                width: calc(100% - 20px);
            }
        }
    </style>
</head>
<body>
    <div class="event-card">
        <div class="title">Event Creation</div>
        <div class="subtitle">Create your event with ease</div>
        <%= form_with(model: @event, local: true) do |form| %>
            <% if @event.errors.any? %>
                <div style="color: red">
                    <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>
                    <ul>
                        <% @event.errors.full_messages.each do |error| %>
                            <li><%= error.full_message %></li>
                        <% end %>
                    </ul>
                </div>
            <% end %>

            <div>
                <%= form.label :name, "Event Name" %>
                <%= form.text_field :name, class: 'form-input', id: 'EventName', required: true %>
            </div>

            <div>
                <%= form.label :venue, "Event Venue" %>
                <%= form.text_field :venue, class: 'form-input', id: 'EventVenue', required: true %>
            </div>

            <div>
                <%= form.label :date, "Event Date" %>
                <%= form.date_field :date, class: 'form-input', id: 'EventDate', required: true %>
            </div>

            <div>
                <%= form.label :max_capacity, "Max Capacity" %>
                <%= form.number_field :max_capacity, class: 'form-input', id: 'MaxCapacity', required: true %>
            </div>

            <div>
                <%= form.label :start_time, "Event Start Time" %>
                <%= form.time_field :start_time, class: 'form-input', id: 'EventStartTime', required: true %>
            </div>

            <div>
                <%= form.label :end_time, "Event End Time" %>
                <%= form.time_field :end_time, class: 'form-input', id: 'EventEndTime', required: true %>
            </div>

            <div>
                <%= form.label :csv_file, "Upload List of Emails (CSV)" %>
                <%= form.file_field :csv_file, class: 'form-input' %>
            </div>

            <br>

            <div>
                <h4 align="center">
                    <%= form.submit "Submit", class: "gradient-btn" %>
                </h4>
            </div>
        <% end %>
    </div>
</body>
</html>
