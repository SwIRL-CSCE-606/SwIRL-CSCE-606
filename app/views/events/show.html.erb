<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #e2c9be;
            background-image: linear-gradient(315deg, #e2c9be 0%, #fbf7e9 74%);
            font-family: 'Arial', sans-serif;
        }
        .event-card {
            background-color: rgba(255, 255, 255, 0.9);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
            border-radius: 15px;
        }
        .gradient-btn {
            background: linear-gradient(45deg, #4CAF50, #44A1F2);
            border: none;
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 30px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            transition: all 0.3s;
        }
        .gradient-btn:hover {
            background: linear-gradient(45deg, #44A1F2, #4CAF50);
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.4);
            transform: translateY(-2px);
        }
        .event-header {
            background-color: #4CAF50;
            color: white;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.2);
        }
        .highlight-text {
            color: rgb(8, 92, 120);
            border-radius: 10px;
            font-weight: bold;
        }
    </style>
</head>
<body>

    <header class="event-header text-center p-4">
        <h1 class="mb-0">Skhedule.com</h1>
    </header>

    <main class="container my-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="event-card p-4">
                    <h3 class="text-center text-success mb-4"><%= notice %></h3>
                    <%= render @event %>
                    <div class="text-center mt-4">
                        <%= button_to "Edit this event", edit_event_path(@event), class: "btn gradient-btn", role: "button" %> 
                        <br>
                        <%= button_to "Event Status", eventsList_path, class: "btn gradient-btn", role: "button" %>
                        <br>
                        <%= button_to "Destroy this event", @event, method: :delete, class: "btn gradient-btn", role: "button", data: { confirm: "Are you sure?" } %>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
