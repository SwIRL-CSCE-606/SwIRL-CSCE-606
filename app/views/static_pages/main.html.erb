<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Event Page</title>
    <!-- Bootstrap CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Arial', sans-serif;
            background-color: black;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .full-screen-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('path-to-your-background-image.jpg') no-repeat center center;
            background-size: cover;
            z-index: -1;
            opacity: 0.5; /* Dim the background image to keep the text prominent */
        }

        .content {
            z-index: 10;
            color: white;
            max-width: 960px;
            padding: 15px;
        }

        .title {
            font-size: 80px; /* Large text size */
            font-weight: bold;
            letter-spacing: -2px; /* Tight letter spacing for effect */
            margin-bottom: 30px;
        }

        .subtitle {
            font-size: 24px;
            margin-bottom: 25px;
        }

        .cta-button {
            background-color: #333; /* Darker background for button */
            color: white;
            border: none;
            padding: 15px 30px;
            font-size: 18px;
            text-transform: uppercase;
            letter-spacing: 2px;
            border-radius: 5px; /* Slightly rounded corners for the button */
            transition: background-color 0.3s, transform 0.3s;
        }

        .cta-button:hover {
            background-color: #444; /* Lighten button background on hover */
            cursor: pointer;
            transform: scale(1.05); /* Slightly enlarge button on hover */
        }

        /* Add more styles as needed */
    </style>
</head>
<body>

    <div class="full-screen-bg"></div>
    
    <div class="content">
        <div class="title">SKHEDULE.COM</div>
        <div class="subtitle">Join us at SKHEDULE.COM and explore a world of events waiting for you!</div>
        <!-- Ruby on Rails button_to helper -->
        <%= button_to "Events Dashboard", home_path, method: :get, class: "cta-button" %>
    </div>

</body>
</html>
