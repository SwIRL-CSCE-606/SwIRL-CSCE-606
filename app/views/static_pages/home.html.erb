<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Skhedule.com - Create Your Event</title>
    <!-- Bootstrap CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Arial', sans-serif;
            background-color: black;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: white;
        }

        .full-screen-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('path-to-your-background-image.jpg') no-repeat center center;
            background-size: cover;
            z-index: -1;
            opacity: 0.5;
        }

        .content {
            z-index: 10;
            max-width: 960px;
            padding: 15px;
        }

        .title {
            font-size: 80px;
            font-weight: bold;
            letter-spacing: -2px;
            margin-bottom: 30px;
        }

        .subtitle {
            font-size: 24px;
            margin-bottom: 25px;
        }

        .cta-button {
            background-color: #333;
            color: white;
            border: none;
            padding: 15px 30px;
            font-size: 18px;
            text-transform: uppercase;
            letter-spacing: 2px;
            border-radius: 5px;
            transition: background-color 0.3s, transform 0.3s;
            margin: 10px; /* Adds space between buttons */
            width: 250px; /* Fixed width for consistency */
            display: block; /* Stack buttons vertically */
            margin-bottom: 20px; /* Space between buttons */
        }

        .cta-button:hover {
            background-color: #444;
            cursor: pointer;
            transform: scale(1.05);
        }

        /* Custom styles for button container */
        .button-container {
            display: flex;
            flex-direction: row; /* Stack buttons vertically */
            align-items: center; /* Center buttons horizontally */
            justify-content: center; /* Center buttons vertically */
            gap: 20px; /* Space between buttons */
        }
    </style>
</head>
<body>

<div class="full-screen-bg"></div>

<div class="content">
    <div class="title">SKHEDULE.COM</div>
    <div class="subtitle">Click below to create events</div>
    <div class="button-container">
        <!-- Ruby on Rails button_to helper for creating singular event -->
        <%= button_to "Create singular event", newEvents_path, method: :get, class: "cta-button" %>
        <!-- Ruby on Rails button_to helper for creating series event -->
        <%= button_to "Create series event", series_path, method: :get, class: "cta-button" %>
        <!-- Ruby on Rails button_to helper for checking current status -->
        <%= button_to "Check current status", eventsList_path, method: :get, class: "cta-button" %>
        <!-- Uncomment the below line if the eventdashboard_path is defined in your routes -->
        <!-- <%= button_to "Event Dashboard", eventdashboard_path, method: :get, class: "cta-button" %> -->
    </div>
</div>
<div>
<%= link_to "Login", signin_path %>


</div>
</body>
</html>
